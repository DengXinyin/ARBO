% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{minmax_normalize}
\alias{minmax_normalize}
\title{Min-max normalize a matrix by columns}
\usage{
minmax_normalize(x)
}
\arguments{
\item{x}{A numeric matrix with samples/pixels as rows and features as columns.}
}
\value{
A numeric matrix of the same dimension as \code{x}, scaled to the
range \code{[0, 1]} for each column.
}
\description{
This function performs min-max normalization for each column independently:
\deqn{(x - min(x)) / (max(x) - min(x))}
}
\details{
Constant columns should be removed before calling this function.
}
\examples{
mat <- matrix(c(1, 2, 3, 10, 20, 30), nrow = 3)
minmax_normalize(mat)

}
